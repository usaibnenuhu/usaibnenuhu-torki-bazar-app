{
  "name": "@torki-bazar/database",
  "version": "0.1.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "node scripts/generate-client.js && tsc -p tsconfig.json",
    "generate": "node scripts/generate-client.js",
    "migrate:dev": "prisma migrate dev",
    "migrate:deploy": "prisma migrate deploy",
    "seed": "ts-node --transpile-only src/seed.ts",
    "studio": "prisma studio",
    "generate:postgres": "prisma generate --schema=prisma/schema.postgres.prisma"
  },
  "dependencies": {
    "@prisma/client": "^5.20.0",
    "@torki-bazar/shared": "*",
    "bcryptjs": "^2.4.3"
  },
  "devDependencies": {
    "prisma": "^5.20.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  },
  "prisma": {
    "seed": "ts-node --transpile-only src/seed.ts"
  }
}
